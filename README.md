# Super Auto Pets · 单机版

一个纯前端的 Super Auto Pets 复刻，**双击 `index.html` 就能玩**。
不需要安装任何东西，不需要命令行，不需要服务器。

---

## 怎么玩

**双击 `index.html`**，浏览器打开即可。

### 操作

| 操作 | 说明 |
|---|---|
| 点击商店里的宠物 | 购买（3 金） |
| 点击队伍中的宠物 | 选中它 |
| 连点同一只宠物两次 | 出售（+1 金） |
| 拖动队伍中的宠物 | 调整站位顺序 |
| 点击道具 | 进入选目标状态，再点一只宠物使用 |
| 🎲 刷新 | 花 1 金重掷商店（1 金） |
| ❄ 冻结 | 该位置的商品下回合保留 |
| ⚔️ 结束回合 | 开打！ |

### 规则

- 每回合 **10 金**，回合结束清零（不累积）
- 宠物 3 金、道具 3 金、刷新 1 金、出售返还 1 金
- 队伍上限 **5 只**
- 买**同名**宠物会合成升级：`3 只 → 2 级`，`再 3 只 → 3 级`
- 每点经验 **+1/+1**
- **10 胜**通关，**3 败**出局（平局不算输）

---

## 内容规模

- **31 个宠物**：Turtle Pack（经典包）Tier 1–3 全部宠物
- **4 种召唤物**：僵尸蟋蟀、脏老鼠、公羊、蜜蜂
- **2 种道具**：苹果（+1/+1）、蜂蜜（阵亡召唤 1/1 蜜蜂）
- 战斗机制完整实现：开战技能、攻击前、互殴、受伤、遗言、召唤、前后排见证、攻击后

---

## 战斗机制（这是引擎的核心）

引擎严格按 SAP 官方规则实现，几条关键点：

1. **攻击是「双方同时互殴」**，不是先手制。
   每个战斗回合，双方最前排的宠物**同时**对彼此造成伤害。
2. **伤害 = 攻击力**，最低 1 点；有西瓜时可减到 0。
3. **阶段顺序**：
   `开战 → 攻击前 → 互殴 → 受伤/阵亡 → 攻击后 → 后排见证 → 回合结束`
4. **宠物阵亡后从队伍移除，后排补位**。
5. **遗言链会递归结算**：一个宠物的阵亡触发的召唤/伤害，可以继续引发新的阵亡。

---

## 代码结构

```
index.html          界面结构
css/style.css       样式
js/data.js          宠物 / 道具数据 ← 加宠物只改这里
js/engine.js        战斗引擎（事件日志驱动）
js/game.js          商店 / 经济 / 回合 / 对手生成
js/ui.js            渲染与交互
```

### 两条关键设计

**1. 引擎输出「有序事件日志」，而不是最终状态**

引擎跑完一场战斗，返回的不是"谁赢了"，而是一串事件：

```js
{ e:'attack', a:uid, b:uid, dmgA:3, dmgB:1 }
{ e:'faint',  t:uid, pos:0 }
{ e:'summon', t:uid, side:0, pos:0, defId:'ZombieCricket' }
```

界面拿着这串事件**逐条播放**，于是血条、动画、召唤都自然发生。
这也意味着**战斗回放功能是白送的**。

**2. 技能是数据，引擎不认识任何具体宠物**

引擎只提供通用原语（`buff` / `hit` / `summon` / `behind` / `ahead` …），
每个宠物的技能写成一小段数据：

```js
Ant: {
  name: 'Ant', tier: 1, atk: 2, hp: 2,
  hooks: {
    faint: (g, c) => {
      const t = g.random(g.friends(c.self), 1);
      if (t[0]) g.buff(t[0], c.lvl, c.lvl);
    }
  }
}
```

**加一只新宠物 = 加一条数据，不用改引擎。**

---

## 加自己的宠物

在 `js/data.js` 的 `PETS` 里加一条即可。可用的 `hooks`：

| 阶段 | hook | 上下文 |
|---|---|---|
| 战斗 | `startOfBattle` | 开战时 |
| 战斗 | `beforeAttack` | 自己即将攻击 |
| 战斗 | `selfAttack` | 自己攻击后 |
| 战斗 | `hurt` | 自己受伤（`c.dmg`） |
| 战斗 | `afterAttack` | 自己攻击结算完毕 |
| 战斗 | `aheadAttack` | 前面的队友攻击了 |
| 战斗 | `aheadFaint` | 前面的队友阵亡了 |
| 战斗 | `faint` | 自己阵亡（遗言） |
| 战斗 | `friendSummoned` | 友方被召唤（`c.target`） |
| 商店 | `buy` / `sell` / `levelUp` | 买 / 卖 / 升级时 |
| 商店 | `startTurn` / `endTurn` | 回合开始 / 结束 |
| 商店 | `friendlyAteFood` | 友方吃了道具（`c.eater`） |

可用原语：`g.buff(pet, atk, hp)`、`g.hit(pet, n)`、`g.summon(side, pos, defId, {atk, hp, lvl})`、
`g.friends(pet)`、`g.foes(pet)`、`g.behind(pet, n)`、`g.ahead(pet, n)`、`g.random(arr, n)`、
`g.givePerk(pet, 'Melon')`、`g.addGold(n)`、`g.stock('Apple', 0)`

---

## 数据来源与已知简化

- **战斗机制**：依据官方规则实现，并参照 MIT 许可的开源实现
  [`koisland/SuperAutoTest`](https://github.com/koisland/SuperAutoTest) 逐项核对
  （攻击同时结算、触发分层、阵亡塌缩、伤害上下限）。
- **宠物数据**：从官方 wiki（`superautopets.wiki.gg`）抓取的真实数值，
  覆盖当前版本的 364 个宠物，本项目选取 Turtle Pack Tier 1–3。

### 本版相对官方做的简化

| 项目 | 简化说明 |
|---|---|
| `Pigeon` 的技能 | 官方库存 Bread Crumbs，本版统一用苹果（同为 +1/+1 类道具） |
| 道具系统 | 只做了苹果和蜂蜜；官方还有西瓜/大蒜等，其中西瓜已在战斗里生效（Ox 会获得） |
| 队伍规模 | 固定 5 只（官方随回合增长到 7 只） |
| 商店等级 | 未实现（官方可花钱提升商店 tier 以解锁高阶宠物） |
| 对手 | 按回合强度**随机生成**，不是真实玩家快照 |
| 玩具 / 困难模式 | 未实现 |

---

## 说明

- 玩法机制不受版权保护，但**美术、名称、商标归 Team Wood Games 所有**。
  本项目的宠物图标使用系统 emoji，无任何官方素材。
- 本项目仅供自己娱乐，未发布、未上线、无联机。
